name: testapp
version: '1.0'
summary: Test app using snap-helpers
description: Test app using snap-helpers
grade: stable
confinement: strict
base: core22

apps:
  testapp:
    command: bin/python3
    plugs:
      - home
      - network
      - network-bind

parts:
  testapp:
    plugin: python
    source: .
    python-packages:
      - ./snap-helpers
      - ./testapp
    override-build: |
      craftctl default
      snap-helpers write-hooks
